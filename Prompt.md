# 需求说明
## 背景
本系统主要解决工业零件设计场景中，新的设计需求产生时，如何快速复用历史上已有设计图纸，即如何通过新需求中局部零件或部分零件的图片，和历史上曾经已有设计图纸（PDF 文件）进行相似度匹配，为新零件的设计提供参考，避免重复造轮子。下文提到的图片均指局部零件或部分零件的图片，图纸均指历史上曾经已有设计图纸（PDF 文件）。

## 图片知识库
在开发系统前，需要搭建一个图纸知识库，要求如下：
在[图纸](./图纸/)目录下是每张图纸经过解析后的结果，主要含 md 文件和 images 目录下的各个视图的图片。你需要对每张图纸进行下列操作：
1. 将 images 目录下各图片使用 qwen3-vl-embedding 模型进行图片的向量化，存储在一个向量数据库 MO 当中，具体的，你需要新建一个表。表名是：view_pic_vec，表的字段含：图纸名称 diagram_name（如：20C114220.pdf）、图片名称 pic_name（如：20C114220_page_full.png）、图片向量化的结果 vec_value
2. 新建一个表。表名是：view_text_vec，表的字段含：图纸名称 diagram_name（如：20C114220.pdf）、分段名称 chunk_name（如：主加工视图）、分段原文 vec_value、分段向量化的结果 vec_value
3. 对 md 文件进行分段，分段规则与[AI 匹配之图形模式](#ai-匹配之图形模式)第 3 步保持一致（图片锚定分段）：
- 依次找到每个图片的链接，对每个链接：
    - 再找到最靠近链接的上方标题，作为该分段的名称 chunk_name（如：加工视图 1：主视图）
    - 再找到属于该标题范围内的所有内容（不可以包含下一个同级或更高级标题、不可以包含图片链接文本）作为分段正文
- 其中文件标题（每个文件唯一的最高一级标题 `#`，对应整页渲染图）不作为分段；
- 若多个图片锚定到同一个标题，只产出一个分段。
4. 对分段的结果使用 text-embedding-v4 模型进行向量化，并将每个分段的结果存储在表 view_text_vec 中
5. 涉及 MO 数据库的操作样例见 [MO 指引](./MO%20创建向量数据表和查询.md)

## 系统功能
### 上传图片
用户可以上传局部零件或部分零件的图片（png、jpg、bpm 等常见格式）。首先，删除[片段](./片段/)目录下的所有内容，将上传的图片文件存放在目录 [upload](./upload/) 中，使用 QWEN 模型对图片的内容进行解读，[样例代码](./SampleCode/ParsePicBase64ViaQwen.py)。如果图片中没有任何机械零件设计的图形，则进行[AI 匹配之文本模式](#ai-匹配之文本模式)，否则，进行[AI 匹配之图形模式](#ai-匹配之图形模式)。

下文对上传的文件名用 XXX 表征。

### AI 匹配之图形模式
- 使用下列命令处理上传的文件，

```
codex --dangerously-bypass-approvals-and-sandbox exec --skip-git-repo-check "对文件 upload\XXX.png 进行内容识别，1、基于零件的每一个加工视图做裁切（如果只有一个视图，则不需要截取，取整个视图即可），并对每个视图中信息(尺寸、弧度)进行提取和对含义进行说明和解读，要求把裁切的图片也嵌入到 Markdown 文件中（采用 UTF-8 编码），并放在对应的位置，即裁切图片要和其被提取的内容在同一个文档区域；请先将 PDF 渲染为整页 PNG，再按每个加工视图、剖面视图、局部视图、表格、NOTES、标题栏分别裁切。裁切后必须检查每张图片是否包含完整尺寸线和文字；如果有边缘文字被裁掉，必须重裁，如果带入了其他视图中的片段（如：尺寸），要严格区分归属，只能提取当前视图中的内容到对应的文档区域，禁止混杂；如果裁切后的内容没有图形，只有文字，直接丢弃；2、表格、文本都要精准还原和提取；3、需要严格还原原图的结构，不做主观整理；4、最终生成 '片段\XXX_extracted.md' 和 '片段\XXX_assets\images\'；5、禁止把整页 PNG 放在 Markdown 文件中；6、禁止把整页 PNG 放在 images 子目录中；7、不要探测或使用 ImageMagick/magick；8、不要探测或使用 tesseract/OCR 命令行工具；9、图像尺寸读取、裁切、保存固定使用 Python + Pillow(PIL)，不要调用 magick identify/convert" 
```

经上述对上传文件的解析后，在[片段](./片段/)目录下，存放的是新需求中局部零件或部分零件的图片，其内容已经被解析为 md 文件和 images 子目录下的各个视图的图片，按照下列规则和图纸知识库中的图纸进行相似度匹配：
1. 基于图片相似度和和文本相似度两个维度进行比对，这两个维度的默认权重分配（支持在页面配置权重）是：图片向量相似度权重占 70%、文本语义相似度占 30%，最后根据不同维度的相似度（采用 cosine_similarity）乘以权重，再求和，获取综合相似度得分。如：图片相似度得分是 60，文本相似度得分是 30，则综合相似度得分是： 60 * 70% + 30 * 30%
2. 依次对[片段](./片段/)目录下，images 子目录的各个图片进行图片相似度检索（采用 cosine_similarity），即检索表 view_pic_vec，取所有行匹配结果中的最大值（即最相似的得分）。如果有多个图片，则每个图片平分权重。如有两个图片，相似度得分分别是 40 和 50，则图片相似度得分是：(40 * 50% + 50 * 50%) * 70%
3. 对 md 文件进行分段，规则如下：
- 依次找到每个图片的链接，对每个链接：
    - 再找到最靠近链接的上方标题
    - 再找到属于该标题范围内的所有内容（不可以包含下一个同级标题、不可以包含链接文本）
4. 对每个分段使用 text-embedding-v4 模型进行向量化，依次使用每个分段的向量化结果去表 view_text_vec 中进行相似度检索（采用 cosine_similarity），取所有行匹配结果中的最大值（即最相似的得分），如果有多个分段，则每个分段平分权重。如有两个分段，相似度得分分别是 40 和 50，则图片相似度得分是：(40 * 50% + 50 * 50%) * 30%
5. 把第 2 步和第 4 步的得分相加，可以获取对每个图纸的相似度综合得分
6. 比对不同图纸的相似度综合得分，给出排名（取 top 3）。对于排名第一的图纸，即最相似的图纸，需要展示：
- [片段](./片段/)目录下，images 子目录中各个图片和哪个图纸视图图片的最相似
- 每个分段中的文本和图纸中哪段文本最相似
7. 在匹配过程中，需要在页面上输出上述步骤 1~5，表示 AI 的思考过程

### AI 匹配之文本模式
1. 以 markdown 格式返回图片中的内容（文本、表格等），存储为'片段\XXX_extracted.md'
2. 对 md 文件使用 text-embedding-v4 模型进行向量化
3. 在表 view_text_vec 中进行相似度检索（采用 cosine_similarity），取相似度最高的图纸,给出排名（取 top 3）,对于排名第一的图纸，即最相似的图纸，需要展示：
- md 文件中的文本和图纸中哪段文本最相似

# 技术栈
## 前端
react
## 后端
- python
- 使用 UV 进行 python 管理，当前环境下只安装一个 python 版本，使用该版本即可