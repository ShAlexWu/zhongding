# Docker 部署脚本生成指南

## 部署环境
ubuntu 24.04.4

## 端口配置
宿主机对外开放端口 8004

## 具体要求
- 使用阿里云源来安装 docker 并配置中国大陆常用的镜像加速器（如果已经安装 docker engine 和 docker compose，则跳过安装，否则进行安装，确保两者都安装成功）
- 前端、后端分别各自部署镜像，为前端和后端各自生成 dockerfile
- 前端容器内的 nginx 端口配置
  - 前端端口是 50009，后端服务端口是 50011（本项目实际端口，与 start.sh 一致）
  - 系统 api 服务接口的路径：健康检查接口路径是 health 是/health；其他接口路径是 /api/...  
- 容器内对于 python、uv、npm 等组件（如果有）的源都统一设置为阿里云
- 由于 .env 中含秘钥，故不纳入 git，且对于 .env 中的各参数的赋值，通过和用户交互写入
- 最终生成一个 .sh 部署脚本