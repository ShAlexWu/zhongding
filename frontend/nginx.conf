# 前端容器内 nginx 监听 50001（宿主机通过 docker-compose 映射到对外的 8001）。
# 后端服务在同一 docker network 里，容器名 backend，监听 50002。

server {
    listen 50001;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # 上传的图片走这里，默认 1m 太小
    client_max_body_size 50m;

    # 健康检查：约定路径 /health（转发到后端的 /health）
    location = /health {
        proxy_pass http://backend:50002/health;
        proxy_set_header Host $host;
    }

    # 系统 API：/api/...
    location /api/ {
        proxy_pass http://backend:50002/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # /api/upload 用 SSE（text/event-stream）流式推送处理进度，
        # 必须关闭代理缓冲，否则前端会卡住直到整个响应结束才收到数据。
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_buffering off;
        proxy_cache off;

        # 深度解读（Codex）等长任务可能耗时数分钟，超时给宽松一点
        proxy_connect_timeout 30s;
        proxy_send_timeout 600s;
        proxy_read_timeout 600s;
    }

    # 片段/图纸缩略图等静态资源
    location /static/ {
        proxy_pass http://backend:50002/static/;
        proxy_set_header Host $host;
    }

    # 前端单页应用路由
    location / {
        try_files $uri $uri/ /index.html;
    }
}
